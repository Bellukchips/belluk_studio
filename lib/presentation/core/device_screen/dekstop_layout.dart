import 'package:flutter/material.dart';

class DekstopScaffold extends StatelessWidget {
  final Widget scaffold;
  const DekstopScaffold({
    Key? key,
    required this.scaffold,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return scaffold;
  }
}
